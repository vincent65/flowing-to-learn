# Core dependencies
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
PyYAML>=6.0

# CLIP embeddings
# Note: CLIP model used for precomputing embeddings (optional at runtime)
# git+https://github.com/openai/CLIP.git

# Data processing
Pillow>=10.0.0

# Optional: for UMAP visualization
# umap-learn>=0.5.0

